# 项目必读说明

本项目是涉案资金证据审查辅助工具。输出用于材料之间的资金对应、覆盖和冲突复核，不替代司法机关对事实、定罪、量刑或犯罪金额的最终认定。

## 先看什么

1. 先看 `README.md`、`START_HERE.md` 和 `REVIEW.md`。
2. 修改前先运行离线测试：`.\\.venv\\Scripts\\python.exe -m pytest -q`。
3. 涉及界面、排序或报告的改动，至少用演示案和 `GOLD_CASE_001` 检查一次签署、刷新恢复和导出。

## DeepSeek 与确定性代码的边界

DeepSeek 只适合辅助处理非结构化语义：

- 从起诉书、笔录等文字中提取付款事实主张；
- 对流水摘要进行语义解释或给出调查建议草稿；
- 把已经由规则引擎确认的风险，转写成便于阅读的说明。

以下内容必须由确定性代码完成，不交给大模型决定：

- 金额加总、差额、精度和净额参考值；
- canonical 交易事件、镜像流水和重复记录识别；
- 账户关系、第三方账户和资金方向判断；
- 匹配规则、风险阻断、复核状态和版本校验；
- 脱敏、SHA-256 指纹、不可变保存和审计留痕。

摘要中出现“收益”“分红”“份额”等文字，不能自动证明返还性质，也不能自动产生法定冲减效果。疑似转回金额只能作为待人工核验参考。

后续优化先把脱敏代码/UI片段交给 DeepSeek 评审，确认问题属于语义能力还是确定性工程问题，再决定是否写代码。未经明确必要性，不为模型已经能可靠完成的语义工作增加本地规则。

## 数据与回查

- 原始上传的 Word、Excel 和其他证据只在当前流程中读取；系统不直接修改原始文件。
- Excel 直接解析，内部标准化文本仅用于流程兼容；原始工作表账户和行号会保留到核查定位中。
- 审查结论中的调查建议应作为可执行回查事项，关联流水号、证据编号和原始行号。完成状态可在页面标记并随 JSON/CSV 清单导出。
- 签署时生成的是脱敏本地 SQLite 检查点，刷新页面可恢复已签署案件；签署不是修改原始证据。

## 当前控制数

`GOLD_CASE_001` 是公开虚构评测案，不是真实案件。原始 Excel 当前应解析为 122 笔记录，候选 18 笔，其中直接账户候选 15 笔、合计 `¥6,118,000.00`；第三方账户争议候选 3 笔、合计 `¥1,250,000.00`；按账户关系识别的疑似转回流水为 7 笔、合计 `¥1,326,000.00`。这些数字用于回归验证，不是法律结论。

## API 密钥

DeepSeek API 只能通过当前进程环境变量 `DEEPSEEK_API_KEY` 配置。密钥不得写入源代码、README、测试材料、日志、SQLite 快照或 Git 提交。默认使用 Mock，避免无意联网和产生费用。
